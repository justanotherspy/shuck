{{define "header"}}<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>shuck token portal</title>
<style>
  body { font-family: system-ui, sans-serif; max-width: 44rem; margin: 4rem auto; padding: 0 1rem; color: #1a1a1a; }
  h1 { font-size: 1.4rem; }
  code, .token { font-family: ui-monospace, monospace; }
  .token { display: block; padding: 1rem; background: #f4f4f4; border: 1px solid #ddd; border-radius: 6px; word-break: break-all; font-size: 1.05rem; }
  .warn { color: #8a5300; }
  .error { color: #a1120b; }
  .muted { color: #666; font-size: 0.9rem; }
  button { padding: 0.5rem 1rem; font-size: 1rem; cursor: pointer; }
  a.button { display: inline-block; padding: 0.5rem 1rem; background: #1a7f37; color: #fff; text-decoration: none; border-radius: 6px; }
</style>
</head>
<body>
<h1>shuck token portal</h1>
{{end}}
{{define "footer"}}</body>
</html>
{{end}}
